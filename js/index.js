// const canvas = document.getElementById('gameCanvas');
const game = new Game(homeWidth, homeHeight, playerAttributes);
// game.init()

// const gameEngine = new GameEngine(canvas);
// gameEngine.init();

// window.addEventListener('keydown', (e) => gameEngine.handleKeyPress(e));
  